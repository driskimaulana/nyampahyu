import { makeStyles } from "@mui/styles";

export default makeStyles(() => (
	{
		list: {
			display: "grid",
			alignItems: "center",
			textAlign: "center",
			justifyContent: "center",
			top: "60vh",
			position: "relative",
			margin: "0px 20px",
		}
	}
))